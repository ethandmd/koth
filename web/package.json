{
  "name": "koth-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:wasm": "wasm-pack build ../rs --target web --out-dir ../web/pkg",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "^8.6.3"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
